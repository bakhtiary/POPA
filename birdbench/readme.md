This directory experiment contains code that runs experiments on birdbench.

To get it working first you have to download the benchmark dataset using the setup and the run the experiment on
all or some selected samples.

```shell
setup.sh
uv run run_experiment.py --select-samples 0:1
```

